# Dependencies (rebuild with `npm install`)
node_modules/

# Build output (rebuild with `npm run dist`)
dist/

# Local user data — these hold real client/session data, keep them OFF GitHub
backups/
*.timing-backup.json
timing-data.json
window-state.json
mini-state.json

# Claude Code local settings
.claude/settings.local.json

# OS / editor cruft
Thumbs.db
Desktop.ini
.DS_Store
